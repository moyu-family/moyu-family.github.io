{
  "name": "moyu-family",
  "version": "1.4.0",
  "private": true,
  "description": "Ứng dụng tĩnh quản lý hồ sơ gia đình (không cần bundler). Bộ script này chỉ hỗ trợ test và kiểm tra trước khi deploy lên GitHub Pages.",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "crypto-js": "^4.2.0",
    "jsdom": "^24.1.3"
  }
}
